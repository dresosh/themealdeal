<div class="container">
  <div class="col-sm-12">
    <div class="panel panel-default">  
      <div class="panel-heading">Admin Page</div>
        <table class="table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Name</th>
              <th>Email</th>
              <th>Vendor Status</th>
              <th>Businessname</th>
              <th>Business Address</th>
              <th>Coordinates (longitude)</th>
              <th>Coordinates (latitude) </th>
              <th>Show </th>
              <th>Edit </th>
              <th>Save </th>
              <th>Delete</th>
            </tr>
          </thead>
            <tbody>
              <% for(var i=0; i < users.length; i++) {  %>
                <tr> 
                  <td><%= users[i].id %></td>       
                  <td><%= users[i].local.name %></td>
                  <td><%= users[i].local.email %></td>
                  <td><button type="text" id= "vendorchange_<%users[i].id%>" class="form-control" name="email"><%= users[i].local.isvendor %></button></td>
                  <td><%= users[i].local.businessname %></td>
                  <td><%= users[i].local.businessaddress %></td>
                  <td><%= users[i].local.longitude %></td>
                  <td><%= users[i].local.latitude %></td>
                  <td>  <button type="button" class="btn btn-default" aria-label="Left Align"> 
                        <%- link_to('show', '../admin/'+users[i].id) %>
                        <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                        </button>  
                  </td>
                  <td>  <button class="btn btn-default" aria-label="Left Align">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>

                        </button>  
                  </td>
                  <td>  <button type="button" class="btn btn-default" aria-label="Left Align">
                        <span class="glyphicon glyphicon-floppy-disk" aria-hidden="true"></span>
                        </button>  
                  </td>
                  <td>  <button type="button" class="btn btn-default" aria-label="Left Align">
                        <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                        </button>  
                  </td>
                </tr> 
              <% } %>
            </tbody>  
        </table>
      </div>
    </div>
  </div>
</div>
        <%- include ./partials/vendorsignup.ejs%>
